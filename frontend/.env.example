# ============================================
# Frontend Environment Configuration
# ============================================

# Backend API URL
# Development: http://localhost:8000
# Production: Set in Vercel environment variables (e.g., https://your-backend.vercel.app)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Better Auth Configuration
# CRITICAL: BETTER_AUTH_SECRET must match backend JWT_SECRET for token verification
# Generate a secure random string for production (at least 32 characters)
BETTER_AUTH_SECRET=your-secure-secret-min-32-chars

# Better Auth URL (the URL where this frontend is hosted)
# Development: http://localhost:3000
# Production: Automatically detected via window.location.origin or VERCEL_URL
BETTER_AUTH_URL=http://localhost:3000

# Database URL for Better Auth (same as backend)
# Required for Better Auth to store session data
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# ============================================
# Vercel Environment Variables (set in dashboard)
# ============================================
# Required for production:
# - BETTER_AUTH_SECRET (must match backend JWT_SECRET)
# - DATABASE_URL (Neon PostgreSQL connection string)
# - NEXT_PUBLIC_API_URL (optional if using hardcoded URL in config.ts)
#
# Auto-provided by Vercel:
# - VERCEL (set to "1" in production)
# - VERCEL_URL (deployment URL without protocol)
